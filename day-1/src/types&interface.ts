interface Person {
    id: number;
    firstname: string;
    lastname: string;
    hobbies?: string[];
}

let person = {
  id: 1,
  firstname: "Prajwal",
  lastname: "Jaiswal",
  hobbies: ["Development", "poetry", "physics"],
};

function getFullname(obj: Person):string  {
  return ``;
}
